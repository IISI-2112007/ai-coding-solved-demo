name: Cloud Agent Simulator

on:
  issues:
    types:
      - opened
      - labeled
      - edited
  workflow_dispatch:
    inputs:
      issue_number:
        description: 要手動模擬的既有 Issue 編號。
        required: true
      title:
        description: 可選的任務標題覆寫。
        required: false
      body:
        description: 可選的任務內容覆寫。
        required: false

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  simulate-cloud-agent:
    name: 模擬 cloud-agent 交接
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.issue.labels.*.name, 'cloud-agent:ready')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 解析 Issue 脈絡
        id: issue
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_ISSUE_NUMBER: ${{ github.event.issue.number }}
          EVENT_ISSUE_TITLE: ${{ github.event.issue.title }}
          EVENT_ISSUE_BODY: ${{ github.event.issue.body }}
          INPUT_ISSUE_NUMBER: ${{ inputs.issue_number }}
          INPUT_TITLE: ${{ inputs.title }}
          INPUT_BODY: ${{ inputs.body }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "number=$INPUT_ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
            echo "title=${INPUT_TITLE:-手動 Cloud Agent Simulator 任務}" >> "$GITHUB_OUTPUT"
            {
              echo "body<<EOF"
              echo "${INPUT_BODY:-由 GitHub Actions 手動觸發。}"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          else
            echo "number=$EVENT_ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
            echo "title=$EVENT_ISSUE_TITLE" >> "$GITHUB_OUTPUT"
            {
              echo "body<<EOF"
              echo "$EVENT_ISSUE_BODY"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: 建立 cloud-agent branch
        run: |
          git config user.name "cloud-agent-simulator[bot]"
          git config user.email "cloud-agent-simulator[bot]@users.noreply.github.com"
          git checkout -b "cloud-agent/issue-${{ steps.issue.outputs.number }}-${{ github.run_id }}"

      - name: 產生可審查輸出
        run: |
          python scripts/cloud_agent_simulator.py \
            --issue-number "${{ steps.issue.outputs.number }}" \
            --issue-title "${{ steps.issue.outputs.title }}" \
            --issue-body "${{ steps.issue.outputs.body }}" \
            --repository "${{ github.repository }}" \
            --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Commit 產出
        run: |
          git add DEMO_RESULTS.md cloud-agent-output/
          git commit -m "cloud agent 產出 issue #${{ steps.issue.outputs.number }}"

      - name: Push branch
        run: |
          git push --set-upstream origin "cloud-agent/issue-${{ steps.issue.outputs.number }}-${{ github.run_id }}"

      - name: 建立 Pull Request
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: cloud-agent/issue-${{ steps.issue.outputs.number }}-${{ github.run_id }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          PR_URL="$(gh pr create \
            --base "$BASE_BRANCH" \
            --head "$BRANCH_NAME" \
            --title "[cloud-agent] 完成 issue #${{ steps.issue.outputs.number }}" \
            --body-file "cloud-agent-output/issue-${{ steps.issue.outputs.number }}/pr-body.md")"
          gh issue comment "${{ steps.issue.outputs.number }}" --body "Cloud Agent Simulator 已接手這個任務，並開啟 PR 交給人類審查：$PR_URL"
